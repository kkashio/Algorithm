/**
 * Created by seongmin Park on 2017. 9. 12..
 */
public class KakaoFestivalPre3 {
    public String solution(String sentence) {
        String answer = "";
        int l = sentence.length();
        char[] str = sentence.toCharArray();
        boolean[] visit = new boolean[l];
        //같은 소문자가 1개거나 3개이상이면 규칙1
        //같은 소문자가 2개 이상이고 중간에 대문자가 있으면 규칙2

        return answer;
    }
}
